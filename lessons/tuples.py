# a tupel can't be changed or modified
# a tupel uses these()but a list uses[]
# tupel are used becuase that datea that is stored in them probley they dont want changed

# this is a tuple 
coordinates = (4, 5)
#this will print 4 it countes from 0 so it uses the index system
print(coordinates[0])

#this will throw a error at you because you can't change the contents of a tupel
'''hello = (8, 9)
hello[1] = 10
print(hello)'''

print(" ")

# this is a list with tuples inside of it you can't change them but you can add more tupkle in it
world = [(6,9), (4,8), (4,20)]
print (world)